# Add-ons

Home Assistant Supervisor add-ons in this repository.

| Slug | Directory | Description |
|------|-----------|-------------|
| `discord_bridge` | [`discord_bridge/`](discord_bridge/) | Discord to n8n bridge |

Each add-on is a self-contained directory with its own `config.yml`, `Dockerfile`, and source code. See the README inside each directory for details.

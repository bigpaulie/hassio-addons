# Changelog

## 1.0.1

- Add `debug` option for verbose add-on logging
- Include `message_id` and `channel_id` in the webhook payload
- Include optional `reply_to` object when forwarding a reply
- Change webhook `author` from a string to an object with `username` and `id`

## 1.0.0

- Initial release
